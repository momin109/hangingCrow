{
  "name": "betting-system",
  "version": "1.0.0",
  "description": "13-role RBAC betting system",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run start:dev",
    "install:all": "cd client && npm install && cd ../server && npm install"
  }
}